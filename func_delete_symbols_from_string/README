Create a function that deletes all the characters that we passed as the second argument from the string.
Example: 'func ("hello world", [' l ',' d '])' will return us "heo wor"

Создать функцию, которая исключает из строки все символы, которые мы передали вторым аргументом.
'func("hello world", ['l', 'd'])' вернет нам "heo wor”
